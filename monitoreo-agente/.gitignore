# Virtual environment
.venv/

# Development data and logs (machine-specific)
data/
logs/

# Site-specific config (copy from config.example.json)
config.json

# Python cache
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# PyArrow / Parquet artifacts
*.parquet
*.snappy

# PyInstaller build artifacts
.venv-build/
build/
deploy/bin/
*.spec

# Release ZIPs (generated by empaquetar.ps1, stored in repo root)
*.zip

# Windows
Thumbs.db
