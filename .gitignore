# Python cache
__pycache__/
*.pyc
*.pyo

# Release packages generated by empaquetar.ps1
monitoreo-agente-v*.zip
*.zip

# Live configs with sensitive hashes — use *.example.yaml as templates
config/tokens.yaml
config/servidor.yaml
config/report.yaml
*.local.yaml

# Local monitoring data (real data, never commit)
data/
logs/
