server:
  host: "0.0.0.0"
  port: 8081
  max_upload_mb: 50

storage:
  # Mismo volumen que el servidor, montado en modo lectura
  base_dir: "/data"
  parquet_subdir: "parquet"
  audit_log_file: "log/ingest.log"
  reject_dir: "rejected"

credentials:
  username: "admin"
  # Hash SHA-256 de la contrasena del dashboard.
  # Generar con:
  #   python3 -c "import hashlib; print(hashlib.sha256(b'TU_CONTRASENA').hexdigest())"
  password_hash: "REEMPLAZAR_CON_HASH_SHA256_DE_LA_CONTRASENA"

logging:
  level: "INFO"
  file: "/data/log/report.log"
  rotate_mb: 50
  backups: 5
